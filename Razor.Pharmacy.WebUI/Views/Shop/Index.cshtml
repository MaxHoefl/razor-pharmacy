@model IEnumerable<Razor.Pharmacy.Domain.Entities.PharmacyItem>

<!DOCTYPE html>

<html>
<head>
    <title>Shop</title>
</head>
<body>
<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 m">
                <div class="card mb-4 box-shadow">
                    <img src=@($"imgs/item{item.ItemId}.png") alt="Pharmacy Item" class="card-img-top">
                    <div class="card-body">
                        <p class="card-text">@item.Name</p>
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <button class="btn btn-dark" type="button">Add to Basket</button>
                            </div>
                            <div>
                                <p>@item.Price €</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
</body>
</html>